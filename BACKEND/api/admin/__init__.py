# api.admin package — all endpoints consumed by the Next.js Admin Panel.
# URL prefix: /admin (set in main.py when including routers).
# Auth: uses ADMIN_SECRET_KEY (different from mobile SECRET_KEY).
# Access: requires role "admin" or "superadmin" on every route.
